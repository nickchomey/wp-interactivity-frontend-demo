/**
 * Toggle Store - Simple Example #2
 *
 * Demonstrates:
 * - data-wp-show (show/hide elements)
 * - data-wp-bind--aria-expanded (accessibility)
 * - Boolean context values
 *
 * No JavaScript needed — the toggle action is an inline expression in HTML.
 */

// No store needed — context mutations are handled by inline expressions.
